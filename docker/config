sudo docker run --name some-redis -p 6379:6379 -d redis

sudo docker network create --driver bridge postgres-network

sudo docker run --name teste-postgres --network=postgres-network -e "POSTGRES_PASSWORD=Postgres2018!" -p 5432:5432 -v /home/hashiguchi/projeto/bases/PostgreSQL:/var/lib/postgresql/data -d postgres

sudo docker run --name teste-pgadmin --network=postgres-network -p 15432:80 -e "PGADMIN_DEFAULT_EMAIL=alshashiguchi@gmail.com" -e "PGADMIN_DEFAULT_PASSWORD=PgAdmin2018!" -d dpage/pgadmin4


sudo docker run --name teste-pgadmin --network=postgres-network -d dpage/pgadmin4


sudo docker exec -it some-redis redis-cli --scan --pattern '*'

 sudo docker exec -it some-redis redis-cli FLUSHALL

sudo docker start teste-pgadmin teste-postgres some-redis  



docker build -t <hub-user>/<repo-name>[:<tag>]
docker tag <existing-image> <hub-user>/<repo-name>[:<tag>]
docker commit <existing-container> <hub-user>/<repo-name>[:<tag>]

Now you can push this repository to the registry designated by its name or tag.

$ docker push <hub-user>/<repo-name>:<tag>